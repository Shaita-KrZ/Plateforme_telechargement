-- Affichage des cartes prépayées déjà existant pour un client donné
SELECT *
FROM CartePrepayee CP
WHERE CP.client = 1; -- A remplacer par une variable

-- Ajout d'une carte prépayée pour un client donné : INSERT INTO
